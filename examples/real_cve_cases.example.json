[
  {
    "id": "example-jwt-verify-false",
    "repo": "owner/python-service",
    "pattern": "verify=False",
    "cwe": "CWE-347",
    "path_hint": "",
    "match": "cwe-347",
    "_note": "Example only. Each case names a real repo + the literal code a real fix removed; the harness derives the (vulnerable, fixed) commit pair from history via git pickaxe and measures whether the bundled detectors catch the vulnerable revision and stay clean on the fix. Never put commit SHAs here — they are derived, not asserted. Replace these with confirmed real-CVE entries and run: AEGIS_REAL_CVE_CASES=this.json python -m aegis.bench.real_cve (in a scanner environment / the arsenal image)."
  },
  {
    "id": "example-php-object-injection",
    "repo": "owner/php-app",
    "pattern": "unserialize($_POST",
    "cwe": "CWE-502",
    "path_hint": "",
    "match": "cwe-502"
  }
]
